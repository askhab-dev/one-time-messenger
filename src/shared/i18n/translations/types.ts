export interface TranslationTypes {
  common: {
    cancel: string;
  };
  header: {
    changeLanguage: string;
    changeTheme: string;
  };
  main: {
    description: string;
    createRoom: string;
    joinRoom: string;
    signIn: string;
  };
  create: {
    title: string;
  };
}
